version: '3.8'

services:
  postgres:
    image: postgres:14.19-alpine3.21
    container_name: postgres-db
    environment:
      POSTGRES_DB: pos_db
      POSTGRES_USER: pos_user
      POSTGRES_PASSWORD: pos_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U pos_user -d pos_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:8.0.5-alpine
    container_name: redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  zookeeper:
    image: confluentinc/cp-zookeeper:7.5.0
    container_name: pos-zookeeper
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    ports:
      - "2181:2181"
    volumes:
      - zookeeper_data:/var/lib/zookeeper/data
      - zookeeper_logs:/var/lib/zookeeper/log

  kafka:
    image: confluentinc/cp-kafka:7.5.0
    container_name: pos-kafka
    depends_on:
      - zookeeper
    ports:
      - "9092:9092"
      - "29092:29092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:29092,PLAINTEXT_HOST://localhost:9092
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"
    volumes:
      - kafka_data:/var/lib/kafka/data
    healthcheck:
      test: ["CMD", "kafka-broker-api-versions", "--bootstrap-server", "localhost:9092"]
      interval: 10s
      timeout: 10s
      retries: 5

  # T112: Order Service for QRIS Guest Ordering
  # order-service:
  #   build:
  #     context: ./backend/order-service
  #     dockerfile: Dockerfile
  #   container_name: order-service
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #     redis:
  #       condition: service_healthy
  #   environment:
  #     # Database
  #     DATABASE_URL: postgresql://pos_user:pos_password@postgres:5432/pos_db?sslmode=disable
  #     DB_HOST: postgres
  #     DB_PORT: 5432
  #     DB_USER: pos_user
  #     DB_PASSWORD: pos_password
  #     DB_NAME: pos_db
      
  #     # Redis
  #     REDIS_URL: redis://redis:6379
  #     REDIS_HOST: redis
  #     REDIS_PORT: 6379
      
  #     # Midtrans Payment Gateway
  #     MIDTRANS_SERVER_KEY: ${MIDTRANS_SERVER_KEY:-your-server-key-here}
  #     MIDTRANS_CLIENT_KEY: ${MIDTRANS_CLIENT_KEY:-your-client-key-here}
  #     MIDTRANS_ENVIRONMENT: ${MIDTRANS_ENVIRONMENT:-sandbox}
      
  #     # Google Maps API for Geocoding
  #     GOOGLE_MAPS_API_KEY: ${GOOGLE_MAPS_API_KEY:-your-google-maps-api-key}
      
  #     # Service URLs
  #     TENANT_SERVICE_URL: ${TENANT_SERVICE_URL:-http://tenant-service:8001}
  #     PRODUCT_SERVICE_URL: ${PRODUCT_SERVICE_URL:-http://product-service:8002}
      
  #     # Server Configuration
  #     PORT: 8080
  #     LOG_LEVEL: ${LOG_LEVEL:-info}
  #     ENVIRONMENT: ${ENVIRONMENT:-development}
      
  #   ports:
  #     - "8080:8080"
  #   volumes:
  #     - ./backend/order-service:/app
  #     - order_service_cache:/go/pkg/mod
  #   healthcheck:
  #     test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8080/health"]
  #     interval: 30s
  #     timeout: 10s
  #     retries: 3
  #     start_period: 40s
  #   restart: unless-stopped
  #   networks:
  #     - pos-network

volumes:
  postgres_data:
  redis_data:
  zookeeper_data:
  zookeeper_logs:
  kafka_data:
  # order_service_cache:

networks:
  pos-network:
    driver: bridge
