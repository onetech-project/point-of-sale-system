# Frontend Configuration

# API Configuration
# Public API URL used by browser (client-side)
NEXT_PUBLIC_API_URL=http://localhost:8080

# API Gateway URL for server-side rewrites (Next.js server)
# Examples:
#   Local: http://localhost:8000
#   VPS: http://api-gateway:8000 (internal Docker network)
#   VPS: https://api.yourdomain.com (external with reverse proxy)
API_GATEWAY_URL=http://localhost:8080

# Storage Configuration (Feature 005 - Product Photos)
# MinIO/S3 endpoint URL for Next.js Image optimization
# Examples:
#   Local: http://localhost:9000
#   VPS: https://minio.yourdomain.com
#   AWS S3: https://s3.amazonaws.com
NEXT_PUBLIC_S3_ENDPOINT=http://localhost:9000

# App Configuration
NEXT_PUBLIC_APP_NAME=Posku
NEXT_PUBLIC_APP_VERSION=1.0.0

# Environment
NEXT_PUBLIC_ENVIRONMENT=development

# Feature Flags
NEXT_PUBLIC_ENABLE_ANALYTICS=false
NEXT_PUBLIC_ENABLE_DEBUG=true

# i18n Configuration
NEXT_PUBLIC_DEFAULT_LOCALE=en
NEXT_PUBLIC_SUPPORTED_LOCALES=en,id
