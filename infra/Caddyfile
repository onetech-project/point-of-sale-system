yourdomain.com {
	# Proxy /api and everything under it to API gateway service on port 8080
	handle_path /api/* {
		reverse_proxy api-gateway:8080
	}

	# All other requests go to the web app on port 3000
	handle /* {
		reverse_proxy frontend:3000
	}
}